import time

import rospy
from std_msgs.msg import Bool


def start_ball_spawning():
    rospy.loginfo("111")
    # pub = rospy.Publisher('/cas_sim/spawn_balls', Bool, queue_size=10)
    #
    # rospy.sleep(10)
    #
    # rospy.loginfo("Publishing message to start spawning balls.")
    #
    # # Create the Bool message
    # msg = Bool()
    # msg.data = True
    #
    # # Publish the message
    # pub.publish(msg)
    #
    # rospy.loginfo("Published message to start spawning balls.")

